apiVersion: v1
kind: Service
metadata:
  name: {{ include "cluster-config.fullname" . }}
  labels:
    {{- include "cluster-config.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "cluster-config.selectorLabels" . | nindent 4 }}
