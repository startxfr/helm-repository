apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "cluster-config.fullname" . }}-test-connection"
  labels:
    {{- include "cluster-config.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "cluster-config.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
