kind: Secret
type: Opaque
apiVersion: v1
metadata:
  name: example
  labels:
    {{- include "example.labels" . | nindent 4 }}
    app.kubernetes.io/name: "{{- include "startx.appName" . -}}-example-{{- include "startx.appVersion" . -}}"
stringData:
  example: {{- .Values.sample_value }}