{{- if .Values.logging.reports -}}{{- if .Values.logging.reports.enabled -}}
{{- $root := . -}}
{{- $namespace := .Values.project.project.name | default "default-logging" -}}
{{- range .Values.logging.reports.list }}
---
kind: Report
apiVersion: logging.openshift.io/v1
metadata:
  name: {{ .name | default "default-report" | quote }}
  namespace: "{{- $namespace -}}"
  labels:
    {{- include "cluster-logging.labels" $root | nindent 4 }}
    app.kubernetes.io/name: "{{ .name | default "default-report" }}-report"
  annotations:
    {{- include "cluster-logging.annotations" $root | nindent 4 }}
spec:
  {{- .spec | nindent 2 }}
{{- end }}
{{- end -}}{{- end -}}