# Default values for configuration of a STARTX cluster
# see values.yaml for explanation on each params
context: &context
  scope: startx
  cluster: localhost
  environment: infra
  component: keycloak
  app: startx-keycloak
keycloak:
  enabled: false
  hooked: false
  name: default
  spec: |
    daemonNodeSelector:
      node-role.kubernetes.io/worker: ""
    # profile:
    # - name: "profile1"
    #   interface: "enp134s0f0"
    #   keycloak4lOpts: "-s -2"
    #   phc2sysOpts: "-a -r"
    # recommend:
    # - profile: "profile1"
    #   priority: 4
    #   match:
    #   - nodeLabel: "node-role.kubernetes.io/worker"
project: 
  enabled: false
  context: 
    <<: *context
  project: 
    enabled: true
    hooked: false
    type: project
    name: "openshift-keycloak"
    display_name: "Startx KEYCLOAK"
    requester: startx
    description: Keycloak configured by STARTX
operator:
  enabled: false
  context: 
    <<: *context
  subscription:
    enabled: true
    hooked: false
    name: "keycloak-operator"
    namespace: "openshift-keycloak"
    version: "v3.3.4"
    operator: 
      channel: keycloak-v3.3
      name: keycloak-operator
      csv: red-hat-keycloak
      source: 
        name: redhat-operators
        namespace: openshift-marketplace
  operatorGroup:
    enabled: true
    hooked: false
    name: "startx-keycloak"
    namespace: "openshift-keycloak"
    providedAPIs: "PtpConfig.v1.keycloak.openshift.io"
