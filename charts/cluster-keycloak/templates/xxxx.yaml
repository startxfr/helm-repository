{{- if .Values.keycloak.enabled -}}
{{- $root := . -}}
{{- $namespace := .Values.project.project.name | default "openshift-keycloak" -}}
{{- $spec := .Values.keycloak.spec | default "{}" -}}
apiVersion: keycloak.openshift.io/v1
kind: PtpConfig
metadata:
  name: {{ .Values.keycloak.name | default "quote" | quote }}
  namespace: "{{- $namespace -}}"
  labels:
    {{- include "cluster-keycloak.labels" . | nindent 4 }}
    app.kubernetes.io/name: "{{- .Values.keycloak.name | default "quote" -}}-keycloakconfig"
  annotations:
    {{- include "cluster-keycloak.annotations" . | nindent 4 }}
    {{- if .Values.keycloak.hooked }}
    helm.sh/hook: post-install,post-upgrade
    helm.sh/hook-weight: "10"
    helm.sh/hook-delete-policy: hook-failed
    argocd.argoproj.io/hook: PostSync
    argocd.argoproj.io/hook-delete-policy: HookFailed
    {{- end }}
    argocd.argoproj.io/sync-wave: "10"
spec:
  {{- $spec | nindent 2 }}
{{- end -}}