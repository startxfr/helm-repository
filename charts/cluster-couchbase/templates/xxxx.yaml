{{- if .Values.couchbase.enabled -}}
{{- $root := . -}}
{{- $namespace := .Values.project.project.name | default "openshift-couchbase" -}}
{{- $spec := .Values.couchbase.spec | default "{}" -}}
apiVersion: couchbase.openshift.io/v1
kind: PtpConfig
metadata:
  name: {{ .Values.couchbase.name | default "quote" | quote }}
  namespace: "{{- $namespace -}}"
  labels:
    {{- include "cluster-couchbase.labels" . | nindent 4 }}
    app.kubernetes.io/name: "{{- .Values.couchbase.name | default "quote" -}}-couchbaseconfig"
  annotations:
    {{- include "cluster-couchbase.annotations" . | nindent 4 }}
    {{- if .Values.couchbase.hooked }}
    helm.sh/hook: post-install,post-upgrade
    helm.sh/hook-weight: "10"
    helm.sh/hook-delete-policy: hook-failed
    argocd.argoproj.io/hook: PostSync
    argocd.argoproj.io/hook-delete-policy: HookFailed
    {{- end }}
    argocd.argoproj.io/sync-wave: "10"
spec:
  {{- $spec | nindent 2 }}
{{- end -}}