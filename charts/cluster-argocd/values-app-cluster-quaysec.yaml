# Auto-generated values-app file for deploying cluster-quay via argocd (security-focused)
# Source: examples/argocd/ from the cluster-quay chart
# Note: Uses values-startx_noinfra-secvuln.yaml for enhanced security scanning

argocd_project:
  enabled: true
  list:
    - name: 'infra-quay'
      namespace: 'openshift-gitops'
      sourceRepos:
        - 'http://sx-helm-repository-prod.s3-website.eu-west-3.amazonaws.com/stable'
      destinations:
        - namespace: 'openshift-gitops'
          server: 'https://kubernetes.default.svc'
        - namespace: 'openshift-startx-quay'
          server: 'https://kubernetes.default.svc'

# Applications
application:
  enabled: true
  list:
    # Application configuration and instances
    - name: 'infra-quaysec-app'
      namespace: 'openshift-gitops'
      finalizer: true
      server: 'https://kubernetes.default.svc'
      project: 'cluster-quay'
      source:
        repo: 'http://sx-helm-repository-prod.s3-website.eu-west-3.amazonaws.com/stable'
        chart: 'cluster-quay'
        rev: '21.3.106'
      helm: |
        valueFiles:
          - values-startx_noinfra-secvuln.yaml
        values: |
          quay:
            enabled: true
          rhn:
            user: "<your-rhn-username>"
            pwd: "<your-rhn-password>"
      syncPolicy: |
        automated:
          prune: true
          selfHeal: true
        syncOptions:
          - CreateNamespace=true
    # Installs and configures the operator
    - name: 'infra-quaysec-operator'
      namespace: 'openshift-gitops'
      finalizer: true
      server: 'https://kubernetes.default.svc'
      project: 'cluster-quay'
      source:
        repo: 'http://sx-helm-repository-prod.s3-website.eu-west-3.amazonaws.com/stable'
        chart: 'cluster-quay'
        rev: '21.3.106'
      helm: |
        valueFiles:
          - values-startx_noinfra-secvuln.yaml
        values: |
          operator:
            enabled: true
      syncPolicy: |
        automated:
          prune: true
          selfHeal: true
    # Creates project namespace and RBAC
    - name: 'infra-quaysec-project'
      namespace: 'openshift-gitops'
      finalizer: false
      server: 'https://kubernetes.default.svc'
      project: 'cluster-quay'
      source:
        repo: 'http://sx-helm-repository-prod.s3-website.eu-west-3.amazonaws.com/stable'
        chart: 'cluster-quay'
        rev: '21.3.106'
      helm: |
        valueFiles:
          - values-startx_noinfra-secvuln.yaml
        values: |
          project:
            enabled: true
      syncPolicy: |
        automated:
          prune: true
          selfHeal: true
        syncOptions:
          - CreateNamespace=true

argocd_repocreds:
  enabled: true
  list: []
