# Auto-generated values-app file for deploying cluster-storage-efs via argocd
# Source: examples/argocd/ from the cluster-storage-efs chart

argocd_project:
  enabled: true
  list:
    - name: 'infra-storage-efs'
      namespace: 'openshift-gitops'
      sourceRepos:
        - 'http://sx-helm-repository-prod.s3-website.eu-west-3.amazonaws.com/stable'
      destinations:
        - namespace: 'openshift-gitops'
          server: 'https://kubernetes.default.svc'
        - namespace: 'openshift-operators'
          server: 'https://kubernetes.default.svc'
        - namespace: 'default'
          server: 'https://kubernetes.default.svc'

# Applications
application:
  enabled: true
  list:
    # Application configuration and instances
    - name: 'infra-storage-efs-app'
      namespace: 'openshift-gitops'
      finalizer: true
      wave: "19"
      server: 'https://kubernetes.default.svc'
      project: 'cluster-storage-efs'
      source:
        repo: 'http://sx-helm-repository-prod.s3-website.eu-west-3.amazonaws.com/stable'
        chart: 'cluster-storage-efs'
        rev: '21.3.106'
      helm: |
        valueFiles:
          - values-startx_noinfra.yaml
        values: |
          operator:
            enabled: false
          sharedVolumes:
            enabled: true
            list:
            - name: startx-aws-efs
              namespace: "default"
              accessPointID: "fsap-xxxxxxxxxxxxxxxxx"
              fileSystemID: "fs-xxxxxxxxxxxxxxxxx"
      syncPolicy: |
        automated:
          prune: true
          selfHeal: true
    # Installs and configures the operator
    - name: 'infra-storage-efs-operator'
      namespace: 'openshift-gitops'
      finalizer: true
      wave: "19"
      server: 'https://kubernetes.default.svc'
      project: 'cluster-storage-efs'
      source:
        repo: 'http://sx-helm-repository-prod.s3-website.eu-west-3.amazonaws.com/stable'
        chart: 'cluster-storage-efs'
        rev: '21.3.106'
      helm: |
        valueFiles:
          - values-startx_noinfra.yaml
        values: |
          operator:
            enabled: true
            operatorGroup:
              enabled: false
          sharedVolumes:
            enabled: false
      syncPolicy: |
        automated:
          prune: true
          selfHeal: true

argocd_repocreds:
  enabled: true
  list: []
