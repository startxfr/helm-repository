{{- if .Values.mtv.enabled -}}
{{- $root := . -}}
{{- $namespace := .Values.project.project.name | default "openshift-mtv" -}}
{{- $spec := .Values.mtv.spec | default "{}" -}}
apiVersion: mtv.openshift.io/v1
kind: PtpConfig
metadata:
  name: {{ .Values.mtv.name | default "quote" | quote }}
  namespace: "{{- $namespace -}}"
  labels:
    {{- include "cluster-mtv.labels" . | nindent 4 }}
    app.kubernetes.io/name: "{{- .Values.mtv.name | default "quote" -}}-mtvconfig"
  annotations:
    {{- include "cluster-mtv.annotations" . | nindent 4 }}
    {{- if .Values.mtv.hooked }}
    helm.sh/hook: post-install,post-upgrade
    helm.sh/hook-weight: "10"
    helm.sh/hook-delete-policy: hook-failed
    argocd.argoproj.io/hook: PostSync
    argocd.argoproj.io/hook-delete-policy: HookFailed
    {{- end }}
    argocd.argoproj.io/sync-wave: "10"
spec:
  {{- $spec | nindent 2 }}
{{- end -}}