{{- if .Values.cerberus.enabled -}}
{{- $root := . -}}
{{- $appName := include "startx.appName" . -}}
{{- $namespace := .Values.cerberus_project.project.name -}}
---
apiVersion: v1
kind: ServiceAccount
metadata:
  name: cerberus
  namespace: {{ $namespace | default "default" | quote }}
  labels:
    app.kubernetes.io/name: "privilegied-cerberus-scc"
    {{- include "chaos-monkey.labels" $root | nindent 4 }}
  annotations:
    {{- include "chaos-monkey.annotations" $root | nindent 4 }}
---
kind: SecurityContextConstraints 
apiVersion: security.openshift.io/v1
metadata:
  name: privilegied-cerberus
  labels:
    app.kubernetes.io/name: "privilegied-cerberus-scc"
    {{- include "chaos-monkey.labels" $root | nindent 4 }}
  annotations:
    {{- include "chaos-monkey.annotations" $root | nindent 4 }}
    kubernetes.io/description: >-
      copy of the OCP 4.9.10 privilegied SCC dedicated to the cerberus SA. 
      Should be updated if any change occur in the default privileged SCC
      in further OCP releases.
users:
- system:serviceaccount:{{- $namespace -}}:cerberus
allowHostPorts: true
priority: null
requiredDropCapabilities: null
allowPrivilegedContainer: true
runAsUser:
  type: RunAsAny
allowHostDirVolumePlugin: true
seccompProfiles:
  - '*'
allowHostIPC: true
seLinuxContext:
  type: RunAsAny
readOnlyRootFilesystem: false
fsGroup:
  type: RunAsAny
defaultAddCapabilities: null
allowedUnsafeSysctls:
  - '*'
supplementalGroups:
  type: RunAsAny
volumes:
  - '*'
allowHostPID: true
allowHostNetwork: true
allowPrivilegeEscalation: true
allowedCapabilities:
  - '*'
---
kind: Role
apiVersion: rbac.authorization.k8s.io/v1
metadata:
  name: privilegied-cerberus-scc-role
  namespace: {{ $namespace | default "default" | quote }}
  labels:
    app.kubernetes.io/name: "privilegied-cerberus-scc-role"
    {{- include "chaos-monkey.labels" $root | nindent 4 }}
  annotations:
    {{- include "chaos-monkey.annotations" $root | nindent 4 }}
rules:
  - apiGroups: ["security.openshift.io"]
    resources: ["securitycontextconstraints"]
    resourceNames: ["privilegied-cerberus"]
    verbs: ["use"]
---
kind: RoleBinding
apiVersion: rbac.authorization.k8s.io/v1
metadata:
  name: privilegied-cerberus-scc-rb
  namespace: {{ $namespace | default "default" | quote }}
  labels:
    app.kubernetes.io/name: "privilegied-cerberus-scc-rb"
    {{- include "chaos-monkey.labels" $root | nindent 4 }}
  annotations:
    {{- include "chaos-monkey.annotations" $root | nindent 4 }}
subjects:
  - kind: ServiceAccount
    name: cerberus
roleRef:
  kind: Role
  name: privilegied-cerberus-scc-role
  apiGroup: rbac.authorization.k8s.io
{{- end -}}
