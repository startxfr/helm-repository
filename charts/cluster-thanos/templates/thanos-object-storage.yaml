apiVersion: v1
kind: Secret
metadata:
  name: thanos-object-storage
type: Opaque
stringData:
  thanos.yaml: |
    type: s3
    config:
      bucket: {{ .Values.thanos.bucket.s3bucket }}
      endpoint: {{ .Values.thanos.bucket.s3host }}
      insecure: true
      access_key: {{ .Values.thanos.bucket.s3accesskey }}
      secret_key: {{ .Values.thanos.bucket.s3secretkey }}