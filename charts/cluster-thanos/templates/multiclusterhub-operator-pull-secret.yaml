apiVersion: v1
stringData:
  .dockerconfigjson: {{ .Values.thanos.clusterPullSecret | quote }}
kind: Secret
metadata:
  name: multiclusterhub-operator-pull-secret
  namespace: open-cluster-management-observability
type: kubernetes.io/dockerconfigjson