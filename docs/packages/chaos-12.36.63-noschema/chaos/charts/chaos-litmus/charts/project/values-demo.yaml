
# Default values for configuration of a dev demo
# see values.keycloak for explanation on each params
context: &context
  scope: startx
  cluster: default
  environment: demo
  component: demo
  app: demo-project
  # version: "0.0.1"  
enabled: true
project: 
  enabled: false
  hooked: false
  type: project
  name: "demo"
  display_name: "Demo default"
  description: "Default default for sxcm components"
